<div id="<%= dom_id(user) %>">
  <p><%= user.email %></p>
  <% if defined?(organisation) %>
    <%= button_to "Remove this user", organisation_organisations_user_path(organisation, user), method: :delete, params: {email: user.email, user_id: user.id} %>
  <% end %>
</div>
