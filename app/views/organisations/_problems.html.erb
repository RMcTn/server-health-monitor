<h1 class="text-xl font-bold">Servers with immediate problems</h1>
<div id="problem_servers" class="flex flex-wrap py-4 pl-4 bg-red-200 rounded-lg">
  <%= turbo_stream_from organisation %>
  <%= render partial: "servers/problem_server", collection: @organisation.servers_with_immediate_problem, as: :server, locals: { organisation: organisation } %>
</div>
