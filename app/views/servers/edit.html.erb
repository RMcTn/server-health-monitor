<div class="w-full mx-auto md:w-2/3">
  <h1 class="text-4xl font-bold">Editing server</h1>

  <%= render "form", server: @server %>

  <%= link_to "Show this server", organisation_server_path(@organisation, @server), class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
  <%= link_to "Back to organisation", organisation_path(@organisation), class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
</div>
